import shutil, sqlite3, os, tempfile; history_db = os.path.expanduser(fr'C:\Users\USERNAME\AppData\Local\Google\Chrome\User Data\Default\History'); temp_history_db = os.path.join(tempfile.gettempdir(), 'History_copy'); shutil.copy(history_db, temp_history_db); conn = sqlite3.connect(temp_history_db); cursor = conn.cursor(); cursor.execute('SELECT url FROM downloads_url_chains'); [print(f'URL: {row[0]}') for row in cursor.fetchall()]; conn.close()